# update-claude-md

Updates the project's `CLAUDE.md` file with info from the conversation, and (if necessary)
explores the codebase and queries the user.

If `CLAUDE.md` does not exist, the skill instructs the agent to use /init.